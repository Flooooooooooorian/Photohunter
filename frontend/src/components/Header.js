import {AppBar, Toolbar} from "@material-ui/core";

export default function Header() {

    return (
        <AppBar>
            <Toolbar>
                <p>
                    PhotoHunter
                </p>
            </Toolbar>
        </AppBar>
    )
}